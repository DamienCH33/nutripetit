when@dev:
    ai:
        platform:
            openai:
                api_key: "%env(OPENAI_API_KEY)%"

when@test:
    ai:
        platform:
            openai:
                api_key: "%env(OPENAI_API_KEY)%"
